name: Running Tests

on: pull_request
jobs:
  Tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: 'Docker Build and Run Tests'
        run: |
          docker build -t test_image .

